int main ( ) { int t , i , n , s , a [ 100 ] , min , count = 0 ; scanf ( "%d" , & t ) ; for ( i = 0 ; i < t ; i ++ ) { scanf ( "%d" , & n ) ; for ( s = 0 ; s < n ; s ++ ) { scanf ( "%d" , & a [ s ] ) ; } min = a [ 0 ] ; for ( s = 0 ; s < n ; s ++ ) { if ( a [ s ] < min ) { min = a [ s ] ; } } count = 0 ; for ( s = 0 ; s < n ; s ++ ) { if ( a [ s ] % min == 0 ) count += 1 ; } for ( s = 0 ; s < n ; s ++ ) { if ( count == n ) printf ( "%d " , a [ s ] / min ) ; else printf ( "%d " , a [ s ] ) ; } printf ( "\n" ) ; } return 0 ; }