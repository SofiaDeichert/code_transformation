int actsum = 0 ; int maxsum ( int * a , int i , int j , int sum ) { sum = sum + a [ i ] ; if ( j * ( j + 1 ) / 2 < a [ 0 ] ) { maxsum ( a , i + j , j + 1 , sum ) ; maxsum ( a , i + j + 1 , j + 1 , sum ) ; } else if ( actsum < sum ) actsum = sum ; return actsum ; } int input ( ) { int a [ 4951 ] , sum = 0 , i , j ; scanf ( "%d" , & a [ 0 ] ) ; a [ 0 ] = ( a [ 0 ] * ( a [ 0 ] + 1 ) ) / 2 ; for ( i = 1 ; i <= a [ 0 ] ; i ++ ) scanf ( "%d" , & a [ i ] ) ; return maxsum ( a , 1 , 1 , 0 ) ; } int main ( ) { int t , b [ 1000 ] , i = 0 ; scanf ( "%d" , & t ) ; for ( i = 0 ; i < t ; i ++ ) { actsum = 0 ; b [ i ] = input ( ) ; } for ( i = 0 ; i < t ; i ++ ) printf ( "%d\n" , b [ i ] ) ; return 0 ; }