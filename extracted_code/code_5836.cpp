main ( ) { long int t ; scanf ( "%ld" , & t ) ; while ( t -- ) { long long int a , b , i ; scanf ( "%lld %lld" , & a , & b ) ; if ( a < b ) { for ( i = a ; i >= 1 ; i -- ) { if ( ( ( a % i ) == 0 ) && ( ( b % i ) == 0 ) ) { printf ( "%lld " , i ) ; break ; } } } else { for ( i = b ; i >= 1 ; i -- ) { if ( ( ( a % i ) == 0 ) && ( ( b % i ) == 0 ) ) { printf ( "%lld " , i ) ; break ; } } } printf ( "%lld\n" , ( a * b ) / i ) ; } }