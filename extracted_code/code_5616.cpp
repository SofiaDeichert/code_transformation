long long int t , n , a [ 500004 ] , i , j , k , tmp , c = 0 ; int main ( ) { scanf ( "%I64d" , & t ) ; while ( t -- ) { scanf ( "%I64d" , & n ) ; for ( i = 0 ; i < n ; i ++ ) { scanf ( "%I64d" , & a [ i ] ) ; } for ( i = n / 2 ; i > 0 ; i = i / 2 ) { for ( j = i ; j < n ; j ++ ) { for ( k = j - i ; k >= 0 ; k = k - i ) { if ( a [ k + i ] >= a [ k ] ) break ; else { tmp = a [ k ] ; a [ k ] = a [ k + i ] ; a [ k + i ] = tmp ; } } } } c = ( a [ 0 ] ) * ( n - 1 ) ; printf ( "%I64d\n" , c ) ; c = 0 ; } return 0 ; }