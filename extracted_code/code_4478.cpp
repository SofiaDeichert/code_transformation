main ( ) { int i , t , mm , n , j , k , b [ 100 ] , a [ 100 ] = { 0 } ; scanf ( "%d" , & t ) ; for ( k = 0 ; k < t ; k ++ ) { scanf ( "%d" , & n ) ; for ( i = 0 ; i < n ; i ++ ) a [ i ] = 0 ; scanf ( "%d" , & b [ 0 ] ) ; a [ 0 ] = b [ 0 ] ; for ( i = 1 ; i < n ; i ++ ) { for ( j = 0 ; j <= i ; j ++ ) scanf ( "%d" , & b [ j ] ) ; a [ i ] = ( a [ i - 1 ] + b [ i ] ) ; for ( j = i - 1 ; j >= 1 ; j -- ) { a [ j ] = ( b [ j ] + ( max ( a [ j ] , a [ j - 1 ] ) ) ) ; } a [ 0 ] += b [ 0 ] ; } mm = 0 ; for ( i = 0 ; i < n ; i ++ ) { if ( a [ i ] > mm ) mm = a [ i ] ; } printf ( "%d\n" , mm ) ; } return 0 ; }