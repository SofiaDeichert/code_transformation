int scan_f ( int * location ) { register int digit ; register int result = 0 ; while ( ( digit = getchar ( ) ) >= '0' ) { result = ( result << 3 ) + ( result << 1 ) + digit - '0' ; } * location = result ; return 0 ; } int row , a [ 100 ] [ 100 ] ; int l ( int n , int m ) { int tlsum , trsum ; row == n ? 0 : ( ( tlsum = l ( n + 1 , m ) ) > ( trsum = l ( n + 1 , m + 1 ) ) ? tlsum : trsum ) ; } int main ( ) { int t , i , m , n , sum ; scan_f ( & t ) ; for ( i = 1 ; i <= t ; i ++ ) { scan_f ( & row ) ; for ( n = 0 ; n < row ; n ++ ) { for ( m = 0 ; m <= n ; m ++ ) { scan_f ( ( * ( a + n ) + m ) ) ; } } sum = l ( 0 , 0 ) ; printf ( "%d\n" , sum ) ; } return 0 ; }