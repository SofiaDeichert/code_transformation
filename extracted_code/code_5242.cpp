int main ( ) { int n , size , i , cost , min_cost = 0 ; scanf ( "%d" , & n ) ; scanf ( "%d" , & size ) ; int a [ size ] ; for ( i = 0 ; i < size ; i ++ ) scanf ( "%d " , & a [ i ] ) ; for ( i = 0 ; i < n ; i ++ ) { while ( size != 1 ) { if ( a [ i ] < a [ i + 1 ] ) for ( i = 0 ; i < size ; i ++ ) { a [ i ] = a [ i + 1 ] ; size = size - 1 ; cost = i ; } else for ( i = 0 ; i < size ; i ++ ) { a [ i + 1 ] = a [ i + 2 ] ; size = size - 1 ; cost = i + 1 ; } min_cost += cost ; } printf ( "%d\n" , min_cost ) ; } }