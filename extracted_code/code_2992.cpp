int main ( ) { int n , A [ 1000 ] , i , j , T ; scanf ( "%d" , & T ) ; while ( T -- ) { scanf ( "%d" , & n ) ; for ( i = 0 ; i < n ; i ++ ) { scanf ( "%d" , & A [ i ] ) ; } int count = 0 , m ; for ( i = 0 ; i < n ; i ++ ) { int k = i ; for ( m = i + 1 ; m < n ; m ++ ) { if ( A [ k ] <= A [ m ] ) { printf ( "%d   %d\n" , A [ k ] , A [ m ] ) ; k = m ; count ++ ; } else { break ; } } } printf ( "%d\n" , count + n ) ; } return 0 ; }