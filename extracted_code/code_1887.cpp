int n ; void bub ( int a [ ] ) { int i , j ; for ( i = 0 ; i < n - 1 ; ++ i ) for ( j = 0 ; j < n - 1 - i ; ++ j ) if ( a [ j ] > a [ j + 1 ] ) a [ j ] = a [ j ] + a [ j + 1 ] - ( a [ j + 1 ] = a [ j ] ) ; } int main ( ) { int t , i ; long int m ; scanf ( "%d" , & t ) ; while ( t -- ) { scanf ( "%d" , & n ) ; int a ; scanf ( "%d" , & m ) ; for ( i = 1 ; i < n ; ++ i ) { scanf ( "%d" , & a ) ; if ( a < m ) m = a ; } m = m * ( n - 1 ) ; printf ( "%ld\n" , m ) ; } return 0 ; }