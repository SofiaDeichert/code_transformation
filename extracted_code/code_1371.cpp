int main ( ) { int t ; int * sol , * temp ; scanf ( "%d" , & t ) ; sol = ( int * ) malloc ( sizeof ( int ) * ( t + 1 ) ) ; temp = sol ; while ( t -- ) { int n ; int i ; scanf ( "%d" , & n ) ; short int a [ 5050 ] ; for ( i = 0 ; i < n * ( n + 1 ) / 2 ; i ++ ) scanf ( "%hd" , & a [ i ] ) ; while ( n ) { for ( i = ( n - 2 ) * ( n - 1 ) / 2 ; i < n * ( n - 1 ) / 2 && i >= 0 ; i ++ ) if ( ( a [ n + i - 1 ] ) >= a [ n + i ] ) a [ i ] += a [ n + i - 1 ] ; else a [ i ] += a [ n + i ] ; n -- ; } ; * temp = a [ 0 ] ; temp ++ ; } * temp = - 1 ; while ( * sol > 0 ) { printf ( "%d\n" , * sol ) ; sol ++ ; } return 0 ; }