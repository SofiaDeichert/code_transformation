int maxab ( int a , int b ) { return a > b ? a : b ; } int main ( int argc , char * argv [ ] ) { int rows , max , cases , i , j , k ; int psum [ 101 ] , nsum [ 101 ] ; scanf ( "%d" , & cases ) ; while ( cases -- ) { psum [ 0 ] = psum [ 100 ] = 0 ; scanf ( "%d" , & rows ) ; for ( i = 1 ; i <= rows ; i ++ ) { for ( j = 1 ; j <= i ; j ++ ) { scanf ( "%d" , & nsum [ j ] ) ; nsum [ j ] += maxab ( psum [ j - 1 ] , psum [ j ] ) ; } for ( j = 1 ; j <= i ; j ++ ) { psum [ j ] = nsum [ j ] ; } } max = nsum [ rows ] ; for ( i = 1 ; i < rows ; i ++ ) max = maxab ( max , nsum [ i ] ) ; printf ( "%d\n" , max ) ; } return 0 ; }