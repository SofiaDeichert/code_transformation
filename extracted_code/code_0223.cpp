int k , a [ 50000 ] ; void delet ( int ) ; int main ( ) { static int cost [ 10 ] ; int n , i , m , t ; scanf ( "%d" , & t ) ; for ( m = 0 ; m < t ; m ++ ) { scanf ( "%d" , & n ) ; k = n ; for ( i = 0 ; i < n ; i ++ ) scanf ( "%d" , & a [ i ] ) ; for ( i = 0 ; i < n - 1 ; i ++ ) { if ( a [ 0 ] < a [ 1 ] ) { cost [ m ] = cost [ m ] + a [ 0 ] ; delet ( a [ 1 ] ) ; } else { cost [ m ] = cost [ m ] + a [ 1 ] ; delet ( a [ 0 ] ) ; } } } for ( m = 0 ; m < t ; m ++ ) printf ( "%d\n" , cost [ m ] ) ; } void delet ( int b ) { int i ; if ( b == a [ 0 ] ) { for ( i = 0 ; i < k - 1 ; i ++ ) a [ i ] = a [ i + 1 ] ; } else { for ( i = 1 ; i < k - 1 ; i ++ ) a [ i ] = a [ i + 1 ] ; } }