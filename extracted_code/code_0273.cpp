int gcd ( int a , int b ) ; int main ( ) { int n , i , a , b ; scanf ( "%d" , & n ) ; for ( i = 0 ; i < n ; i ++ ) { scanf ( "%d %d" , & a , & b ) ; printf ( "%d %d" , gcd ( a , b ) , a * b / gcd ( a , b ) ) ; } return 0 ; } int gcd ( int a , int b ) { int i , min , gcd ; if ( a >= b ) { min = b ; } else { min = a ; } for ( i = min ; i > 0 ; i -- ) { if ( ( a % i ) == 0 && ( b % i ) == 0 ) { gcd = i ; break ; } } return gcd ; }