int main ( ) { int a [ 100 ] [ 100 ] ; int n , num , i , j , colsum [ 100 ] , rowsum [ 100 ] , dsum [ 100 ] , max = 0 ; scanf ( "%d" , & n ) ; for ( ; n ; n -- ) { scanf ( "%d" , & num ) ; for ( i = 0 ; i < 100 ; i ++ ) colsum [ i ] = rowsum [ i ] = dsum [ i ] = 0 ; for ( i = 0 ; i < num ; i ++ ) { for ( j = 0 ; j <= i ; j ++ ) { scanf ( "%d" , & a [ i ] [ j ] ) ; } } for ( i = 0 ; i < num ; i ++ ) { for ( j = i ; j < num ; j ++ ) { colsum [ i ] += a [ j ] [ i ] ; } } for ( i = 0 ; i < num ; i ++ ) { for ( j = 0 ; j <= i ; j ++ ) { rowsum [ i ] += a [ i ] [ j ] ; } } for ( i = 0 ; i < num ; i ++ ) { for ( j = 0 ; j < num ; j ++ ) { dsum [ i ] += a [ j + i ] [ j ] ; } } max = colsum [ 0 ] ; for ( i = 0 ; i < num ; i ++ ) { if ( colsum [ i ] > max ) max = colsum [ i ] ; if ( rowsum [ i ] > max ) max = rowsum [ i ] ; if ( dsum [ i ] > max ) max = dsum [ i ] ; } printf ( "%d\n" , max ) ; } return 0 ; }