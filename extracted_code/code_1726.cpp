int main ( ) { int t , i , p , q , r , j , k , s , n , m ; int a [ 8 ] ; scanf ( "%d" , & t ) ; while ( t -- ) { scanf ( "%d %d" , & p , & q ) ; if ( p < q ) r = p ; else r = q ; s = 0 ; if ( p < q ) m = q ; else m = p ; for ( i = 1 ; i <= r ; i ++ ) { j = r % i ; n = m % i ; if ( ( j == 0 ) && ( n == 0 ) ) { a [ s ] = i ; s ++ ; } } printf ( "%d\n" , a [ s - 1 ] ) ; k = p * q / a [ s - 1 ] ; printf ( "%d\n" , k ) ; } return 0 ; }