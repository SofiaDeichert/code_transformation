int main ( ) { int t , n , i , j , k , a [ 100 ] [ 100 ] , max , m1 , m2 ; scanf ( "%d" , & t ) ; scanf ( "%d\n" , & n ) ; while ( t -- ) { i = 0 ; for ( i = 0 ; i < n ; i ++ ) { for ( j = 0 ; j <= i ; j ++ ) { scanf ( "%d" , & a [ i ] [ j ] ) ; } printf ( "\n" ) ; } max = 0 ; m1 = 0 ; m2 = 0 ; for ( i = 0 ; i < n ; i ++ ) { for ( j = 0 ; j <= i ; j ++ ) { if ( max < a [ i ] [ j ] ) max = a [ i ] [ j ] ; } } for ( i = 0 ; i < n ; i ++ ) { for ( j = 0 ; j <= i ; j ++ ) { if ( a [ i ] [ j ] < max && a [ i ] [ j ] > m1 ) m1 = a [ i ] [ j ] ; } } for ( i = 0 ; i < n ; i ++ ) { for ( j = 0 ; j <= i ; j ++ ) { if ( a [ i ] [ j ] < m1 && a [ i ] [ j ] > m2 ) m2 = a [ i ] [ j ] ; } } printf ( "%d" , max + m1 + m2 ) ; } }