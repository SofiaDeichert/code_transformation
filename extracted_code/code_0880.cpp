int main ( ) { int t , n , i , j , m ; scanf ( "%d" , & t ) ; while ( t ) { scanf ( "%d" , & n ) ; if ( n > 1 ) { int a [ n ] ; j = 0 ; for ( i = 0 ; i < n ; i ++ ) scanf ( " %d" , & a [ i ] ) ; while ( n > 1 ) { if ( a [ 0 ] < a [ 1 ] ) { m = a [ 0 ] ; i = 1 ; } else { m = a [ 1 ] ; i = 0 ; } for ( ; i < n ; i ++ ) a [ i ] = a [ i + 1 ] ; j += m ; n -- ; } printf ( "%d" , j ) ; t -- ; } } return 0 ; }