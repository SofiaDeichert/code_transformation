main ( ) { long int i , n , min ; int t , min_sum ; scanf ( "%d" , & t ) ; while ( t -- ) { scanf ( "%ld" , & n ) ; long int a [ n ] ; for ( i = 0 ; i < n ; i ++ ) scanf ( "%ld" , & a [ i ] ) ; min = 10 ^ 5 ; for ( i = 0 ; i < n ; i ++ ) if ( min > a [ i ] ) min = a [ i ] ; min_sum = min * ( n - 1 ) ; printf ( "%d\n" , min_sum ) ; } }