int main ( ) { int T ; scanf ( "%d" , & T ) ; while ( T -- ) { int a , b , lcm , gcd , i ; gcd = 0 ; lcm = 1 ; scanf ( "%d%d" , & a , & b ) ; ( a > b ) ? ( a = a + b , b = a - b , a = a - b ) : ( a ) ; for ( i = 1 ; i <= a / 2 ; i ++ ) if ( a % i == 0 && b % i == 0 ) gcd = i ; if ( b % a == 0 ) gcd = a ; for ( i = 2 ; i <= a ; i ++ ) { if ( a % i == 0 && b % i == 0 ) { a = a / i ; b = b / i ; lcm *= i ; i -- ; } } lcm *= ( a * b ) ; printf ( "%d %d\n" , gcd , lcm ) ; } }