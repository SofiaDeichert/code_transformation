int main ( ) { int t , n , a , z [ 50000 ] , s [ 50000 ] , i = 0 , c = 0 , p , b ; scanf ( "%d" , & t ) ; for ( b = 1 ; b <= t ; b ++ ) { scanf ( "%d" , & n ) ; i = 0 ; c = 0 ; for ( a = 0 ; a < n ; a ++ ) { scanf ( "%d" , & z [ a ] ) ; s [ a ] = z [ a ] ; } for ( p = 0 ; p < n - i ; p ++ ) { if ( z [ p ] > z [ p + 1 ] ) { c = z [ p + 1 ] + c ; i = i + 1 ; for ( a = 0 ; a < p ; a ++ ) { s [ a ] = z [ a ] ; } for ( a = p + 1 ; a < n - i ; a ++ ) { s [ a ] = z [ a ] ; } } else { c = z [ p ] + c ; i = i + 1 ; for ( a = 0 ; a < p + 1 ; a ++ ) { s [ a ] = z [ a ] ; } for ( a = p + 2 ; a < n - i ; a ++ ) { s [ a ] = z [ a ] ; } } } printf ( "%d" , c ) ; } return 0 ; }