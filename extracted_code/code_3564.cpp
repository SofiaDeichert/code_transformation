int main ( void ) { int t , n , i , inner , outer , temp , cost ; char array [ 50001 ] ; scanf ( " %d" , & t ) ; while ( t -- ) { scanf ( " %d" , & n ) ; for ( i = 0 ; i < n ; i ++ ) { scanf ( " %d" , & array [ i ] ) ; } for ( outer = 0 ; outer < n - 1 ; outer ++ ) { for ( inner = outer ; inner < n ; inner ++ ) { if ( array [ inner ] < array [ outer ] ) { temp = array [ inner ] ; array [ inner ] = array [ outer ] ; array [ outer ] = temp ; } } } cost = ( n - 1 ) * ( array [ 0 ] ) ; printf ( "%d\n" , cost ) ; } return 0 ; }