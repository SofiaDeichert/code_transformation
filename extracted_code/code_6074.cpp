int main ( ) { int n , s , m , i , j ; scanf ( "%d\n" , & n ) ; while ( n > 0 ) { scanf ( "%d" , & s ) ; int a [ 3 ] [ 3 ] ; for ( i = 0 ; i < s ; i ++ ) { printf ( "ds" ) ; for ( j = 0 ; j < i + 1 ; j ++ ) { scanf ( "%d" , & a [ i ] [ j ] ) ; } } int sum = 0 ; for ( i = 0 ; i < s ; i ++ ) { int max = a [ i ] [ 0 ] ; for ( j = 1 ; j < i + 1 ; j ++ ) { if ( max < a [ i ] [ j ] ) max = a [ i ] [ j ] ; } sum = sum + max ; } printf ( "%d\n" , sum ) ; n -- ; } return 0 ; }