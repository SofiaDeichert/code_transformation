int min ( long long int a , long long int b ) ; main ( ) { int t ; long long int n , i , c , e , f , j , temp ; scanf ( "%d" , & t ) ; while ( t -- ) { f = 0 ; scanf ( "%lld" , & n ) ; long long int a [ n ] ; for ( i = 0 ; i < n ; i ++ ) { scanf ( "%lld" , & a [ i ] ) ; } for ( i = 0 ; i < n ; i ++ ) { for ( j = i + 1 ; j < n ; j ++ ) { if ( a [ i ] > a [ j ] ) { temp = a [ i ] ; a [ i ] = a [ j ] ; a [ j ] = temp ; } } } c = min ( a [ 0 ] , a [ 1 ] ) ; e = c ; for ( i = 2 ; i < n ; i ++ ) { c = min ( c , a [ i ] ) ; f = f + c ; } printf ( "%lld\n" , f + e ) ; } return 0 ; } int min ( long long int a , long long int b ) { if ( a > b ) return ( b ) ; else return ( a ) ; }