int main ( ) { int n , l , i , j , k , sum = 0 , maxsum = 0 ; int a [ 101 ] [ 101 ] ; scanf ( "%d" , & n ) ; while ( n > 0 ) { scanf ( "%d" , & l ) ; for ( i = 0 ; i < l ; i ++ ) { for ( j = 0 ; j <= i ; j ++ ) { scanf ( "%d" , & a [ i ] [ j ] ) ; } } k = l - 2 ; j = l - 2 ; while ( k >= 0 ) { i = l - 2 ; while ( i >= 0 ) { if ( a [ i + 1 ] [ j + 1 ] > a [ i + 1 ] [ j ] ) { sum = sum + a [ i ] [ j ] + a [ i + 1 ] [ j + 1 ] ; j -- ; } else { sum = sum + a [ i ] [ j ] + a [ i + 1 ] [ j ] ; j -- ; } i -- ; } if ( maxsum < sum ) { maxsum = sum ; } j = k -- ; k -- ; } printf ( "%d\n" , maxsum ) ; n -- ; } return 0 ; }