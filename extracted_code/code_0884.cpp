void remov ( int a [ ] , int size , int x ) { int i , k ; int flag = 0 ; for ( i = 0 ; i < size ; i ++ ) if ( a [ i ] == x ) { flag = 1 ; break ; } if ( flag == 1 ) { for ( k = i ; k < size - 1 ; k ++ ) a [ k ] = a [ k + 1 ] ; } } int main ( ) { long long int t , n , i , count , a [ 100004 ] ; scanf ( "%lld" , & t ) ; while ( t -- ) { count = 0 ; scanf ( "%d" , & n ) ; for ( i = 0 ; i < n ; i ++ ) { scanf ( "%lld" , & a [ i ] ) ; } while ( a [ 1 ] != '\0' ) { if ( a [ 0 ] < a [ 1 ] ) { count = count + a [ 0 ] ; remov ( a , sizeof ( a ) / 4 , a [ 1 ] ) ; } else { count = count + a [ 1 ] ; remov ( a , sizeof ( a ) / 4 , a [ 0 ] ) ; } } printf ( "%lld\n" , count ) ; } }