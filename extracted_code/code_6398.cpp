int main ( ) { int T , c ; long int a [ 50000 ] , i , N , min ; scanf ( "%d" , & T ) ; while ( T -- ) { c = 0 ; scanf ( "%ld" , & N ) ; for ( i = 0 ; i < N ; i ++ ) scanf ( "%ld " , & a [ i ] ) ; min = a [ 0 ] ; for ( i = 1 ; i < N ; i ++ ) { if ( min > a [ i ] ) min = a [ i ] ; } for ( i = 0 ; i < N ; i ++ ) { if ( a [ i ] > min ) c ++ ; } printf ( "%ld\n" , ( c * min ) ) ; } return 0 ; }