int main ( void ) { long int t , n , num [ 60000 ] , min , i , j , k , c ; scanf ( "%ld" , & t ) ; for ( i = 0 ; i < t ; i ++ ) { scanf ( "%ld" , & n ) ; for ( j = 0 ; j < n ; j ++ ) { num [ j ] = 0 ; } for ( j = 0 ; j < n ; j ++ ) { scanf ( "%ld" , & num [ j ] ) ; } min = num [ 0 ] ; c = n ; for ( k = 1 ; k < c ; k ++ ) { if ( num [ k ] < min ) min = num [ k ] ; c -- ; } printf ( "%ld\n" , min * ( n - 1 ) ) ; } return 0 ; }