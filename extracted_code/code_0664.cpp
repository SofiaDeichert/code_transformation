int main ( ) { int n ; scanf ( "%d" , & n ) ; while ( n -- ) { int n1 , i , j , k = 1 , sum = 0 , s1 = 0 , max = 0 ; scanf ( "%d" , & n1 ) ; int * * a = ( int * * ) malloc ( n1 * sizeof ( int * ) ) ; for ( i = 1 ; i <= n1 ; i ++ ) { a [ i ] = ( int * ) malloc ( i * sizeof ( int ) ) ; for ( j = 1 ; j <= i ; j ++ ) { scanf ( "%d" , & a [ i ] [ j ] ) ; } } for ( i = n1 - 1 ; i > 0 ; i -- ) { for ( j = 1 ; j <= i ; j ++ ) { a [ i ] [ j ] += max ( a [ i + 1 ] [ j ] , a [ i + 1 ] [ j + 1 ] ) ; } free ( a [ i + 1 ] ) ; } printf ( "%d\n" , a [ 1 ] [ 1 ] ) ; free ( a [ 1 ] ) ; free ( a ) ; } }