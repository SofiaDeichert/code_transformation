int main ( ) { int n , m , i , j , a [ 100 ] [ 100 ] , sum , sum1 , fsum = 0 ; sum = a [ 0 ] [ 0 ] , sum1 = a [ 0 ] [ 0 ] ; scanf ( "%d" , & n ) ; while ( n -- ) { scanf ( "%d" , & m ) ; for ( i = 0 ; i < m ; i ++ ) { for ( j = 0 ; j <= i ; j ++ ) { scanf ( "%d" , & a [ i ] [ j ] ) ; } } for ( i = 0 ; i < m ; i ++ ) { for ( j = 0 ; j <= i ; j ++ ) { sum = sum + a [ i + 1 ] [ j ] ; sum1 = sum1 + a [ i + 1 ] [ j + 1 ] ; if ( sum > sum1 ) fsum = sum ; else fsum = sum1 ; } } } printf ( "%d\n" , fsum ) ; return 0 ; }