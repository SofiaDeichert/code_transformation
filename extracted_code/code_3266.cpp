int main ( ) { int t , n , i , k , j , sum , sum1 = 0 ; scanf ( "%d" , & t ) ; int b [ t ] ; for ( k = 0 ; k < t ; k ++ ) { scanf ( "%d" , & n ) ; int a [ n ] [ n ] ; if ( n > 0 && n < 100 ) { for ( i = 0 ; i < n ; i ++ ) { for ( j = 0 ; j < i + 1 ; j ++ ) scanf ( "%d" , & a [ i ] [ j ] ) ; } sum = a [ 0 ] [ 0 ] ; for ( i = 0 ; i < n - 1 ; i ++ ) { for ( j = 0 ; j < i + 1 ; j ++ ) { if ( a [ i + 1 ] [ j ] > a [ i + 1 ] [ j + 1 ] ) sum = sum + a [ i + 1 ] [ j ] ; else sum = sum + a [ i + 1 ] [ j + 1 ] ; } if ( sum > sum1 ) { sum1 = sum ; sum = 0 ; } } b [ k ] = sum1 ; } } for ( i = 0 ; i < t ; i ++ ) printf ( "%d\n" , b [ i ] ) ; return 0 ; }