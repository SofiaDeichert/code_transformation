int main ( ) { int * * a , tests , i , j , n ; scanf ( "%d" , & tests ) ; while ( tests -- ) { scanf ( "%d" , & n ) ; a = malloc ( n * sizeof ( int * ) ) ; for ( i = 0 ; i < n ; i ++ ) a [ i ] = malloc ( ( i + 1 ) * sizeof ( int * * ) ) ; for ( i = 0 ; i < n ; i ++ ) { for ( j = 0 ; j < i + 1 ; j ++ ) { scanf ( "%d" , & a [ i ] [ j ] ) ; } } for ( i = n - 2 ; i >= 0 ; i -- ) { for ( j = 0 ; j < i + 1 ; j ++ ) { a [ i ] [ j ] += ( a [ i + 1 ] [ j ] > a [ i + 1 ] [ j + 1 ] ) ? a [ i + 1 ] [ j ] : a [ i + 1 ] [ j + 1 ] ; } } printf ( "%d\n" , a [ 0 ] [ 0 ] ) ; for ( i = 0 ; i < n ; i ++ ) free ( a [ i ] ) ; free ( a ) ; } return 0 ; }