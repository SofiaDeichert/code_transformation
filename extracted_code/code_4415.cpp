int main ( ) { long int n , a [ 100000 ] , i , j , temp ; int T ; scanf ( "%d" , & T ) ; for ( ; T > 0 ; T -- ) { scanf ( "%ld" , & n ) ; for ( i = 0 ; i < n ; i ++ ) scanf ( "%ld" , & a [ i ] ) ; for ( i = 0 ; i < n ; i ++ ) { for ( j = i + 1 ; j < n ; j ++ ) { if ( a [ i ] > a [ j ] ) { temp = a [ i ] ; a [ i ] = a [ j ] ; a [ j ] = temp ; } } } if ( n == 1 ) printf ( "\n%ld" , a [ 0 ] ) ; else printf ( "\n%ld" , a [ n - 2 ] ) ; } return 0 ; }