long gcd ( long a , long b ) ; int main ( ) { int i , t , x ; long a , b ; scanf ( "%d\n" , & t ) ; for ( i = 0 ; i < t ; i ++ ) { scanf ( "%ld\t%ld" , & a , & b ) ; x = gcd ( a , b ) ; printf ( "%d\t%ld" , x , ( a * b ) / x ) ; } return 0 ; } long gcd ( long a , long b ) { long i , m ; if ( a > b ) { m = b ; } else { m = a ; } for ( i = m ; i > 0 ; i -- ) { if ( a % i == 0 && b % i == 0 ) { return i ; } } }