int bin ( int no ) { int m = 0 ; while ( no > 1 ) { no = no / 2 ; m = m + 1 ; } if ( m > 0 ) return m ; else return 0 ; } int binary ( int n ) { int k = 0 , h = 0 , b , c , i ; while ( n > 0 ) { k = bin ( n ) ; b = 1 ; for ( i = 0 ; i < k ; i ++ ) b = 10 * b ; h = h + b ; c = 1 ; for ( i = 0 ; i < k ; i ++ ) c = 2 * c ; n = n - c ; } return h ; } int main ( ) { int n , i , j , a [ 20 ] [ 20 ] , t , s , d , b , y , x , c , q , w ; scanf ( "%d" , & q ) ; for ( w = 0 ; w < q ; w ++ ) { scanf ( "%d" , & n ) ; for ( i = 0 ; i < n ; i ++ ) { for ( j = 0 ; j < i + 1 ; j ++ ) { scanf ( "%d" , & a [ i ] [ j ] ) ; } } t = 1 ; for ( i = 1 ; i < n ; i ++ ) t = t * 2 ; d = 0 ; for ( i = 0 ; i < t ; i ++ ) { s = a [ 0 ] [ 0 ] ; b = binary ( i ) ; y = 0 ; for ( x = 1 ; x < n ; x ++ ) { c = b ; b = b / 10 ; y = y + c ; s = s + a [ x ] [ y ] ; } if ( s > d ) d = s ; } printf ( "%d\n" , d ) ; } return 0 ; }