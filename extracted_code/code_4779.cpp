int main ( ) { long int t , a [ 100001 ] , i , n , sum , comp ; scanf ( "%ld" , & t ) ; while ( t -- ) { scanf ( "%ld" , & n ) ; for ( i = 0 ; i < n ; i ++ ) scanf ( "%ld" , & a [ i ] ) ; comp = a [ 0 ] ; a [ 0 ] = 1 ; for ( i = 1 ; i < n ; i ++ ) { if ( a [ i ] >= comp ) { comp = a [ i ] ; a [ i ] = a [ i - 1 ] + 1 ; } else { comp = a [ i ] ; a [ i ] = 1 ; } } sum = 0 ; for ( i = 0 ; i < n ; i ++ ) { sum += a [ i ] ; } printf ( "%ld\n" , sum ) ; } return 0 ; }