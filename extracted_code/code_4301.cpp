int input ( int ) ; int ar_order ( int * , int ) ; int sub_array_count ( int * , int ) ; int main ( ) { int arr_sz ; printf ( "Enter The Size Of the array:" ) ; scanf ( "%d" , & arr_sz ) ; assert ( arr_sz >= 2 ) ; input ( arr_sz ) ; return 0 ; } int input ( int arr_sz ) { int inpt_arr [ arr_sz ] ; int i ; for ( i = 0 ; i < arr_sz ; i ++ ) { scanf ( "%d" , & inpt_arr [ i ] ) ; } ar_order ( inpt_arr , arr_sz ) ; } int ar_order ( int * inpt_arr , int arr_sz ) { int i , j , max = 0 ; for ( i = 0 ; i < arr_sz ; i ++ ) { for ( j = i + 1 ; j < arr_sz ; j ++ ) { if ( inpt_arr [ i ] > inpt_arr [ j ] ) { max = inpt_arr [ i ] ; inpt_arr [ i ] = inpt_arr [ j ] ; inpt_arr [ j ] = max ; } } } sub_array_count ( inpt_arr , arr_sz ) ; } int sub_array_count ( int * nw_arr , int arr_sz ) { int i , j , count = 0 ; for ( i = 0 ; i < arr_sz ; i ++ ) { ++ nw_arr [ i ] ; for ( j = i + 1 ; j < arr_sz ; j ++ ) { if ( nw_arr [ i ] == nw_arr [ j ] ) count ++ ; } } printf ( "Number Of Subsets= %d" , count + arr_sz ) ; }