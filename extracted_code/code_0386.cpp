int * * a ; long int maxsum = 0 ; void calc ( int n , int x , int r , long int tsum ) { long int ssum ; int i ; for ( i = x ; i <= r && i < ( x + 2 ) ; i ++ ) { ssum = tsum ; ssum += a [ r ] [ i ] ; if ( r == ( n - 1 ) ) { if ( ssum > maxsum ) maxsum = ssum ; } else { calc ( n , i , r + 1 , ssum ) ; } } } int main ( ) { int t , n , i , j ; scanf ( "%d" , & t ) ; while ( t -- ) { scanf ( "%d" , & n ) ; a = ( int * * ) malloc ( sizeof ( int * ) * n ) ; for ( i = 0 ; i < n ; i ++ ) { a [ i ] = ( int * ) malloc ( sizeof ( int ) * ( i + 1 ) ) ; for ( j = 0 ; j <= i ; j ++ ) { scanf ( "%d" , & a [ i ] [ j ] ) ; } } calc ( n , 0 , 1 , a [ 0 ] [ 0 ] ) ; printf ( "%ld\n" , maxsum ) ; } return 0 ; }