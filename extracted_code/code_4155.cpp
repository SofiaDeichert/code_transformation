int main ( ) { int n , i , j , k , l , sum , max , num , m ; scanf ( "%d" , & num ) ; int arr2 [ num ] ; for ( m = 0 ; m < num ; m ++ ) { scanf ( "%d" , & n ) ; int arr [ n ] [ n ] ; int arr1 [ n ] ; for ( i = 0 ; i < n ; i ++ ) { for ( j = 0 ; j < n ; j ++ ) { if ( j <= i ) { scanf ( "%d" , & arr [ i ] [ j ] ) ; } else { arr [ i ] [ j ] = 0 ; } } } for ( j = 0 ; j < n ; j ++ ) { sum = 0 ; for ( i = 0 ; i < n ; i ++ ) { sum = sum + arr [ i ] [ j ] ; } arr1 [ j ] = sum ; } max = arr1 [ 0 ] ; for ( i = 1 ; i < n ; i ++ ) { if ( max < arr1 [ i ] ) { max = arr1 [ i ] ; } } arr2 [ m ] = max ; } for ( m = 0 ; m < num ; m ++ ) { printf ( "%d\n" , arr2 [ m ] ) ; } return 0 ; }