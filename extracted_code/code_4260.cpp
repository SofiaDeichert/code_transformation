int main ( ) { int i , p , q , t , n , a [ 5050 ] ; scanf ( "%d" , & t ) ; while ( t -- ) { scanf ( "%d" , & n ) ; i = n * ( n + 1 ) / 2 ; while ( i -- ) scanf ( "%d" , & a [ i ] ) ; for ( i = n ; i > 0 ; i -- ) { p = i * ( i + 1 ) / 2 ; q = i * ( i - 1 ) / 2 ; while ( n > 1 ) { if ( a [ p ] > a [ p - 1 ] ) a [ q ] += a [ p ] ; else a [ q ] += a [ p - 1 ] ; p -- ; q -- ; n -- ; } } printf ( "%d\n" , a [ 0 ] ) ; } return 0 ; }