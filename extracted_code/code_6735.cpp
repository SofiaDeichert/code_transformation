int main ( ) { int t , n , i , j , k , no ; long a [ 100005 ] , count ; scanf ( "%d" , & t ) ; while ( t ) { count = k = j = 0 ; scanf ( "%d" , & n ) ; for ( i = 0 ; i < n ; i ++ ) { scanf ( "%ld" , & a [ i ] ) ; } for ( j = 0 ; j < n ; ) { no = 0 ; for ( i = j ; i < n ; i ++ ) { k = i ; if ( a [ i ] <= a [ i + 1 ] ) { no ++ ; } else break ; } if ( no ) count += ( no * ( no + 1 ) ) / 2 ; if ( no > 1 ) j = k ; else j ++ ; } printf ( "%ld\n" , count ) ; t -- ; } return 0 ; }