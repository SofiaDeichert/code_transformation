int main ( void ) { int t , i , j , k , size , l , m , q ; scanf ( "%d" , & t ) ; for ( k = 0 ; k < t ; k ++ ) { scanf ( "%d" , & size ) ; long int a [ size ] ; for ( i = 0 ; i < size ; i ++ ) { scanf ( "%ld" , & a [ i ] ) ; } long int min = 0 ; for ( i = 0 ; i < size - 1 ; i ++ ) { if ( a [ 0 ] > a [ 1 ] ) { min = min + a [ 1 ] ; for ( l = 0 ; l < size - 1 ; l ++ ) a [ l ] = a [ l + 1 ] ; } else { min = min + a [ 0 ] ; for ( m = 1 ; m < size - 1 ; m ++ ) a [ m ] = a [ m + 1 ] ; } } printf ( "%ld\n" , min ) ; } return 0 ; }