int main ( ) { int t , i ; long long int a , b ; scanf ( "%d" , & t ) ; while ( t -- ) { scanf ( "%lld %lld" , & a , & b ) ; int g = 1 ; long long int l = 1 ; if ( a == 0 || b == 0 ) printf ( "0 0\n" ) ; else if ( a > 0 && b > 0 ) { if ( a > b ) { for ( i = 2 ; i <= ( a / 2 ) ; i ++ ) { if ( a % i == 0 && b % i == 0 ) g = g * i ; } } else { for ( i = 2 ; i <= ( b / 2 ) ; i ++ ) { if ( a % i == 0 && b % i == 0 ) g = g * i ; } } l = ( ( a * b ) / g ) ; printf ( "%d %lld\n" , g , l ) ; } } return 0 ; }