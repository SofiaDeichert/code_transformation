void cost ( long long int [ ] , long long int ) ; int main ( ) { long long int a [ 50000 ] , n ; int i , no ; scanf ( "%d" , & no ) ; for ( i = 0 ; i < no ; i ++ ) { scanf ( "%d" , & n ) ; cost ( a , n ) ; } return 0 ; } void cost ( long long int a [ ] , long long int n ) { long long int min , i ; for ( i = 0 ; i < n ; i ++ ) { scanf ( "%ll" , & a [ i ] ) ; } min = a [ 0 ] ; for ( i = 1 ; i < n ; i ++ ) { if ( a [ i ] < min ) min = a [ i ] ; } printf ( "%ll" , ( n - 1 ) * min ) ; return ; }