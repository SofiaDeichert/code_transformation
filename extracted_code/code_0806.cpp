int main ( ) { int * p , * q ; int * r , * s ; int i , n , j ; scanf ( "%d" , & n ) ; printf ( "Done taking n\n" ) ; q = ( int * ) malloc ( sizeof ( int ) * ( n + 1 ) ) ; p = ( int * ) malloc ( sizeof ( int ) * ( n + 1 ) ) ; r = ( int * ) malloc ( sizeof ( int ) * ( n + 1 ) ) ; s = ( int * ) malloc ( sizeof ( int ) * ( n + 1 ) ) ; for ( i = 1 ; i <= ( n ) ; i ++ ) { printf ( "I am inputting value for the %d time\n" , i ) ; scanf ( "%d %d" , & p [ i ] , & q [ i ] ) ; } printf ( "Done inputting\n" ) ; for ( i = 1 ; i <= n ; i ++ ) { printf ( "I came here\n" ) ; if ( p [ i ] < q [ i ] ) { for ( j = p [ i ] ; j >= 1 ; j -- ) { if ( ( p [ i ] % j == 0 ) && ( q [ i ] % j == 0 ) ) { s [ i ] = j ; break ; } } } else { for ( j = q [ i ] ; j >= 1 ; j -- ) { if ( ( p [ i ] % j == 0 ) && ( q [ i ] % j == 0 ) ) { s [ i ] = j ; break ; } } } } for ( i = 1 ; i <= n ; i ++ ) { printf ( "And here too\n" ) ; if ( p [ i ] > q [ i ] ) { for ( j = p [ i ] ; j <= ( p [ i ] * q [ i ] ) ; j ++ ) { if ( ( j % p [ i ] == 0 ) && ( j % q [ i ] == 0 ) ) { r [ i ] = j ; break ; } } } if ( p [ i ] < q [ i ] ) { for ( j = q [ i ] ; j <= ( p [ i ] * q [ i ] ) ; j ++ ) { if ( ( j % p [ i ] == 0 ) && ( j % q [ i ] == 0 ) ) { r [ i ] = j ; break ; } } } } for ( i = 1 ; i <= ( n ) ; i ++ ) { printf ( "%d %d\n" , s [ i ] , r [ i ] ) ; } return 0 ; }