void shell_sort ( int arr [ ] , int length ) ; int main ( ) { int a [ 100000 ] , t , n , i , j , s ; scanf ( "%d" , & t ) ; if ( t >= 1 && t <= 10 ) { while ( t -- ) { s = 0 ; scanf ( "%d" , & n ) ; if ( n >= 1 && n <= 50000 ) { for ( i = 0 ; i < n ; i ++ ) scanf ( "%d" , & a [ i ] ) ; shell_sort ( a , n ) ; while ( n != 1 ) { if ( a [ 0 ] < a [ 1 ] ) { s += a [ 0 ] ; for ( j = 1 ; j < n ; j ++ ) a [ j ] = a [ j + 1 ] ; } n -- ; } printf ( "%d\n" , s ) ; } } } return 0 ; } void shell_sort ( int arr [ ] , int length ) { int i , gap , j , key ; for ( gap = ( length / 2 ) ; gap > 0 ; gap /= 2 ) { for ( i = gap ; i <= length - 1 ; i ++ ) { key = arr [ i ] ; for ( j = i ; j >= gap && arr [ j - gap ] > key ; j -= gap ) arr [ j ] = arr [ j - gap ] ; arr [ j ] = key ; } } }