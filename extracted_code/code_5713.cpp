void del_element ( long a [ ] , int n , int pos ) { int i ; for ( i = pos ; i < n - 1 ; i ++ ) { a [ i ] = a [ i + 1 ] ; } } long find_min ( long a [ ] , int n ) { int i ; long min = a [ 0 ] ; for ( i = 1 ; i < n ; i ++ ) { if ( a [ i ] < min ) { min = a [ i ] ; } } return min ; } int main ( ) { int i , t , n ; scanf ( "%d" , & t ) ; while ( t > 0 ) { scanf ( "%d" , & n ) ; long a [ n ] ; for ( i = 0 ; i < n ; i ++ ) { scanf ( "%ld" , & a [ i ] ) ; } if ( n == 2 ) { if ( a [ 0 ] < a [ 1 ] ) { printf ( "%ld\n" , a [ 0 ] ) ; } else { printf ( "%ld\n" , a [ 1 ] ) ; } } else { if ( a [ 0 ] > a [ 1 ] ) { del_element ( a , n , 1 ) ; } else { del_element ( a , n , 0 ) ; } n -- ; printf ( "%ld\n" , find_min ( a , n ) ) ; } t -- ; } return 0 ; }