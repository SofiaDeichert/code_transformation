long gcd ( long a , long b ) ; int main ( ) { long n , i , a , b ; scanf ( "%ld" , & n ) ; for ( i = 0 ; i < n ; i ++ ) { scanf ( "%ld %ld" , & a , & b ) ; printf ( "%ld %ld" , gcd ( a , b ) , a * b / gcd ( a , b ) ) ; } return 0 ; } long gcd ( long a , long b ) { long i , min , gcd ; if ( a >= b ) { min = b ; } else { min = a ; } for ( i = min ; i > 0 ; i -- ) { if ( ( a % i ) == 0 && ( b % i ) == 0 ) { gcd = i ; break ; } } return gcd ; }