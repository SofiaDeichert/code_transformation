int main ( ) { long long int a [ 500000 ] , m , n , i , j , keys , t ; scanf ( "%lld " , & t ) ; while ( t -- ) { scanf ( "%lld " , & n ) ; for ( i = 0 ; i < n ; i ++ ) { scanf ( "%lld " , & a [ i ] ) ; } for ( i = 1 ; i < n ; i ++ ) { keys = a [ i ] ; j = i - 1 ; while ( j >= 0 && a [ j ] > keys ) { a [ j + 1 ] = a [ j ] ; j -- ; } a [ j + 1 ] = keys ; } if ( n == 2 ) { printf ( "%lld\n" , ( a [ 0 ] ) ) ; } else { m = a [ 0 ] * ( n - 1 ) ; printf ( "%lld\n" , m ) ; } } return 0 ; }