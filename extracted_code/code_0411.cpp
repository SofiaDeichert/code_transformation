int main ( ) { int t ; scanf ( "%d" , & t ) ; int i , m , z = 0 ; int a [ 2 ] ; int b [ 2 ] ; while ( t -- ) { for ( i = 0 ; i < 2 ; i ++ ) { scanf ( "%d" , & a [ i ] ) ; } for ( m = 1000000 ; m > 0 ; m -- ) { for ( i = 0 ; i < 2 ; i ++ ) { b [ i ] = a [ i ] % m ; { if ( b [ i ] != 0 ) { break ; } if ( i == 1 ) { z = m ; printf ( "%d %d\n" , z , ( a [ 0 ] * a [ 1 ] ) / z ) ; } } } } } }