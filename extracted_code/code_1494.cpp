int main ( ) { long int t , n , i , m , j ; scanf ( "%ld" , & t ) ; while ( t -- ) { scanf ( "%ld" , & n ) ; long int a [ n ] [ n ] ; for ( i = 0 ; i < n ; i ++ ) { for ( j = 0 ; j < n ; j ++ ) { if ( i >= j ) scanf ( "%ld" , & a [ i ] [ j ] ) ; } } for ( i = n ; i > 0 ; i -- ) { for ( j = 0 ; j < n ; j ++ ) { if ( i >= j ) { { if ( a [ i ] [ j ] > a [ i ] [ j + 1 ] ) m = a [ i ] [ j ] ; else m = a [ i ] [ j + 1 ] ; a [ i - 1 ] [ j ] += m ; } } } } printf ( "%d\n" , a [ 0 ] [ 0 ] ) ; } return 0 ; }