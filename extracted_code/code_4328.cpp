int max ( int a , int b ) { return a > b ? a : b ; } int main ( ) { int i , j , k ; int n ; scanf ( "%d" , & n ) ; while ( n -- ) { int nrows ; scanf ( "%d" , & nrows ) ; int a [ nrows ] [ nrows ] ; for ( i = 0 ; i < nrows ; i ++ ) { for ( j = 0 ; j < nrows ; j ++ ) { a [ i ] [ j ] = 0 ; } } for ( i = 0 ; i < nrows ; i ++ ) { for ( j = 0 ; j < i + 1 ; j ++ ) { scanf ( "%d" , & a [ i ] [ j ] ) ; } } for ( i = 1 ; i < nrows ; i ++ ) { a [ i ] [ 0 ] += a [ i - 1 ] [ 0 ] ; } for ( i = 1 ; i < nrows ; i ++ ) { for ( j = 1 ; j < i + 1 ; j ++ ) { if ( i == j ) { a [ i ] [ j ] = a [ i ] [ j ] + a [ i - 1 ] [ j - 1 ] ; continue ; } else a [ i ] [ j ] = a [ i ] [ j ] + max ( a [ i - 1 ] [ j - 1 ] , a [ i - 1 ] [ j ] ) ; } } int max = a [ nrows - 1 ] [ 0 ] ; for ( j = 1 ; j < nrows ; j ++ ) { if ( a [ nrows - 1 ] [ j ] > max ) max = a [ nrows - 1 ] [ j ] ; } printf ( "%d\n" , max ) ; } return 0 ; }