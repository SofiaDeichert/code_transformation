int main ( ) { int t ; long int i , j , k ; scanf ( "%d" , & t ) ; while ( t -- ) { long int n , c ; scanf ( "%ld" , & n ) ; c = n ; long int a [ n ] ; for ( i = 0 ; i < n ; i ++ ) scanf ( "%ld" , & a [ i ] ) ; for ( i = 0 ; i < n - 1 ; i ++ ) { for ( j = i + 1 ; j < n ; j ++ ) { int flag = 1 ; for ( k = i ; k < j ; k ++ ) { if ( a [ k ] > a [ k + 1 ] ) { flag = 0 ; break ; } } if ( flag == 1 ) c ++ ; } } printf ( "%ld\n" , c ) ; } return 0 ; }