long long int min ( long long int a , long long int b ) { if ( a > b ) return b ; else return a ; } int main ( ) { int t ; scanf ( "%d" , & t ) ; while ( t -- ) { long long int p , q , l , g , i ; scanf ( "%lld %lld" , & p , & q ) ; if ( p > q && p % q == 0 ) { g = q ; l = p ; } else if ( q > p && q % p == 0 ) { g = p ; l = q ; } else { for ( i = 1 ; i < min ( p , q ) ; i ++ ) { if ( p % i == 0 && q % i == 0 ) g = i ; } l = p * q ; } printf ( "%lld %lld\n" , g , l ) ; } return 0 ; }