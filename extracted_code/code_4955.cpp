int main ( ) { int t , i , n , j , pre ; long co = 0 , * a ; printf ( "Enter test cases\n" ) ; scanf ( "%d" , & t ) ; while ( t > 0 && t <= 5 ) { printf ( "Enter N\n" ) ; scanf ( "%d" , & n ) ; if ( n < 1 || n > 100000 ) break ; a = ( long int * ) malloc ( sizeof ( long int ) * n ) ; printf ( "Enter Array-\n" ) ; for ( i = 0 ; i < n ; i ++ ) { scanf ( "%ld" , & a [ i ] ) ; if ( a [ i ] > alimit || a [ i ] < 1 ) exit ( 0 ) ; } for ( i = 0 ; i < n ; i ++ ) { pre = i ; for ( j = i ; j < n ; j ++ ) { if ( a [ j ] >= a [ pre ] ) co ++ ; if ( j != i ) pre = j ; } } printf ( "%ld" , co ) ; t -- ; } return 0 ; }