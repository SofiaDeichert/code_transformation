int main ( ) { int a , b , x , lcm [ 1000 ] , gcd [ 1000 ] , i , n , j ; scanf ( "%d" , & n ) ; for ( j = 0 ; j < n ; j ++ ) { scanf ( "%d%d" , & a , & b ) ; x = b * ( a > b ) + a * ( a < b ) ; for ( i = 1 ; i <= x ; i ++ ) { if ( a % i == 0 && b % i == 0 ) { gcd [ j ] = i ; } } x = b * ( a < b ) + a * ( a > b ) ; for ( i = x ; i <= ( a * b ) ; i ++ ) { if ( i % a == 0 && i % b == 0 ) { lcm [ j ] = i ; break ; } } } for ( j = 0 ; j < n ; j ++ ) { printf ( "%d %d\n" , gcd [ j ] , lcm [ j ] ) ; } return 0 ; }