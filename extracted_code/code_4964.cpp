void swap ( int * x , int * y ) ; int main ( ) { int t , n , i , j , min , key ; scanf ( "%d" , & t ) ; while ( t -- ) { scanf ( "%d" , & n ) ; int a [ n ] ; for ( i = 0 ; i < n ; i ++ ) { scanf ( "%d" , & a [ i ] ) ; } for ( i = 1 ; i < n ; i ++ ) { key = a [ i ] ; for ( j = i - 1 ; j >= 0 ; j -- ) { if ( key < a [ j ] ) a [ j + 1 ] = a [ j ] ; else break ; } a [ j + 1 ] = key ; } printf ( "%d\n" , a [ 0 ] * ( n - 1 ) ) ; } return 0 ; } void swap ( int * x , int * y ) { int temp = * x ; * x = * y ; * y = temp ; }