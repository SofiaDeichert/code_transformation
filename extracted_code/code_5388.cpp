int newsum [ 102 ] ; int sum [ 102 ] ; void max ( int * a , int i , int n ) { int j ; for ( j = 0 ; j <= i ; j ++ ) { scanf ( "%d" , & a [ j ] ) ; } newsum [ 0 ] = sum [ 0 ] + a [ 0 ] ; for ( j = 1 ; j < i ; j ++ ) { if ( sum [ j - 1 ] > sum [ j ] ) newsum [ j ] = sum [ j - 1 ] + a [ j ] ; else newsum [ j ] = sum [ j ] + a [ j ] ; } newsum [ i ] = a [ i ] + sum [ i - 1 ] ; for ( j = 0 ; j <= i ; j ++ ) { sum [ j ] = newsum [ j ] ; } } int main ( ) { int trial , n , i ; scanf ( "%d" , & trial ) ; while ( trial -- ) { scanf ( "%d" , & n ) ; int a [ n ] ; scanf ( "%d" , & a [ 0 ] ) ; sum [ 0 ] = a [ 0 ] ; for ( i = 1 ; i < n ; i ++ ) sum [ i ] = 0 ; for ( i = 1 ; i < n ; i ++ ) { max ( a , i , n ) ; } int m = 0 ; for ( i = 0 ; i < n ; i ++ ) { if ( sum [ i ] > m ) { m = sum [ i ] ; } } printf ( "%d\n" , m ) ; } return 0 ; }