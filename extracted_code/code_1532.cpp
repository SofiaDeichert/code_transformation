int main ( ) { int t , n , a [ 101 ] [ 101 ] , p , q ; scanf ( "%d" , & t ) ; while ( t -- ) { scanf ( "%d" , & n ) ; for ( p = 0 ; p < n ; p ++ ) { for ( q = 0 ; q <= p ; q ++ ) { scanf ( "%d" , & a [ p ] [ q ] ) ; } } for ( p = n - 1 ; p > 0 ; p -- ) { for ( q = 0 ; q < p ; q ++ ) { if ( a [ p ] [ q ] > a [ p ] [ q + 1 ] ) { a [ p - 1 ] [ q ] = a [ p ] [ q ] + a [ p - 1 ] [ q ] ; } else { a [ p - 1 ] [ q ] = a [ p ] [ q + 1 ] + a [ p - 1 ] [ q ] ; } } } printf ( "%d\n" , a [ 0 ] [ 0 ] ) ; } return 0 ; }