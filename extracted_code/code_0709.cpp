void main ( ) { int t ; scanf ( "%d" , & t ) ; while ( t -- > 0 ) { long int n , i , a [ 100000 ] , j , k = 0 ; scanf ( "%ld" , & n ) ; for ( i = 0 ; i < n ; i ++ ) scanf ( "%ld" , & a [ i ] ) ; while ( 1 ) { for ( i = 0 ; i < n - 1 ; i ++ ) if ( a [ i ] <= a [ i + 1 ] ) { k = k + a [ i ] ; for ( j = i + 1 ; j < n ; j ++ ) a [ j ] = a [ j + 1 ] ; n -- ; } else { k = k + a [ i + 1 ] ; for ( j = i ; j < n ; j ++ ) a [ j ] = a [ j + 1 ] ; n -- ; } if ( n == 1 ) { printf ( "%ld\n" , k ) ; break ; } } } return 0 ; }