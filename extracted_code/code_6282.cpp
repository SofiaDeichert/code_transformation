void gl ( int p , int q ) { int i , big ; ( p > q ) ? ( big = q ) : ( big = p ) ; for ( i = big ; i > 0 ; i -- ) { if ( ( p % i ) == 0 && ( q % i ) == 0 ) { printf ( "%d " , i ) ; break ; } } printf ( "%d \n" , ( p * q ) / i ) ; } int main ( ) { int t ; int i , big , p , q ; scanf ( "%d" , & t ) ; while ( t -- ) { scanf ( "%d" , & q ) ; scanf ( "%d" , & p ) ; gl ( p , q ) ; } return 0 ; }